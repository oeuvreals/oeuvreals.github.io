# 로컬에서 미리보기 할 때만 필요합니다 (GitHub Pages는 자체 빌드).
# 로컬 미리보기: gem install bundler → bundle install → bundle exec jekyll serve
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins

# Windows / Ruby 3+ 호환용
gem "webrick"
